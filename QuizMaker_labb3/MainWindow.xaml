<Window x:Class="QuizMaker_labb3.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:QuizMaker_labb3" xmlns:View="clr-namespace:QuizMaker_labb3.View"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600">
    <Window.InputBindings>
        <KeyBinding Key="U" Modifiers="Ctrl" 
                    Command="{Binding PlayerViewModel.UpdateButtonCommand}" />
    </Window.InputBindings>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        
        <View:MenuView />

        <!--<View:PlayerView  Grid.Row="1"/>-->
        <View:ConfigurationView  Grid.Row="1"/>
        


    </Grid>
</Window>
